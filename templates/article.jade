
extends layout

block nav
  include partials/nav

block prepend title
  | #{ page.title + " - "}

block header
  h1= page.title
  p
    | #{ "Written by " + page.metadata.author }

block content
  article.article
    section.content!= typogr(page.html).typogrify()